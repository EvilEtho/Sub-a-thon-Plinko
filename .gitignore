node_modules/
out/
dist/
*.log
.env
.DS_Store
Thumbs.db

# Local editor/tooling data (plans, session state, launch config) — never commit
.claude/

# Private setup / release runbooks (keep the how-to-configure docs out of the public repo)
RELEASING.md
